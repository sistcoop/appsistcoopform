'use strict';

/* jshint -W098 */
angular.module('formulario').controller('FormularioController', ['$scope', 'Auth',
  function ($scope, Auth) {

    $scope.package = {
      name: 'formulario'
    };

  }
]);
